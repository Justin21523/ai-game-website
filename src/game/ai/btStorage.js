// Centralized storage key(s) for AI Behavior Tree data.
// Keeping these constants in one place prevents subtle mismatches across pages.

// Version the key so future schema migrations can be handled cleanly.
export const BT_STORAGE_KEY = 'bt:platform-brawl:v1'

